import React from "react";
import ValidateEntity from "../Shared/ValidateEntity";

const ValidCrop = ({ navigation }) => (
	<ValidateEntity navigation={navigation} idLabelKey="cropId" headerKey="addCropValidation" />
);

export default ValidCrop;
